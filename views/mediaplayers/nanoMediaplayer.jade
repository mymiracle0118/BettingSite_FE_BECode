<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Player
    style.
      body{
        padding: 0;
        margin: 0;
      }
       
  body

    script.
      document.addEventListener('gesturestart', function (e) {
        e.preventDefault();
      });

    script(src='#{staticUrl}videoplayer/nplayer/js/player.min.js')

    :verbatim
      !{media.app_js}